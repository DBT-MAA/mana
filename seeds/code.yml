version: 2

seeds:
  - name: countrycodedetail
    config:
     column_types:
       COUNTRY: varchar(30)
       COUNTRY_CODE: Number
       codedetails:
        countrycodedetail2:
          column_types:
           COUNTRY: varchar(30)
           COUNTRY_CODE: Number